{
  "name": "api-becker",
  "version": "1.0.0",
  "description": "Repositório para armazenar o código inicial da API do 3º TRIMESTRE. - index.js -> arquivo principal, configura os métodos POST e GET e suas rotas. - db.js -> realiza a conexão com o banco de dados MySQL. - usuario_modelo.json -> fornece um modelo de JSON para facilitar os testes no cliente de API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "mysql2": "^3.14.3"
  }
}
